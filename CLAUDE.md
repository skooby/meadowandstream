# Antigravity Context Rules

All temporary files, scratch scripts, data dumps, or experimental code MUST be saved exclusively within the `.ai_scratch/` directory. Never drop temporary files in the root folder.
