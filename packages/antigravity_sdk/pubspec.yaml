name: antigravity_sdk
description: Antigravity SDK — uses language_server.exe agentapi CLI to spawn and manage agent conversations.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: {}
